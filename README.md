# HyperTerm theme that makes cursor opaque

## How install

Install HyperTerm and add hyperambient to plugins in ~/.hyperterm.js.
```json
 plugins: ["opaque-cursor"],
 ```

Perform a "Full Reload" (or restart HyperTerm) for plugin to take effect

